"""Hivework — full-loop orchestrator package.

Automates the decompose → fan-out → parse → conflict-scan → reconcile → assemble
pipeline using free-tier copilot workers (drones).
"""

__version__ = "0.1.0"
